generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Asset {
  id            String   @id @default(uuid())
  filename      String
  s3Key         String?
  createdAt     DateTime @default(now())
}
