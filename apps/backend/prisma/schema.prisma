generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Asset {
  id            String   @id @default(uuid())
  filename      String
  s3Key         String?
  size          Int
  createdAt     DateTime @default(now())
}
